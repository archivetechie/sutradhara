"""Reconciler spine for desired-state job generation.

The package exposes the generic condition helpers, registry, and control loop
used by P0.3. Importing ``sutradhara.jobs.reconcilers.copy`` registers the first
domain, ``copy``.
"""
